#![allow(dead_code)]
#![allow(unused_imports)]
mod image;
mod filter;

enum EditOptions {
    AddNoise,
    Rotate,
    Resize,
    Crop,
    AddFilter
}

enum Filter {
    Blur,
    EmphasizeBlue,
}

fn main() {

}
